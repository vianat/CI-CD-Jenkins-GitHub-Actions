pipeline {
    agent any

    stages {
        stage('Hello') {
            steps {
                dir ('bugtracker-backend') {
                    go test -v ./...
                }
            }
        }
    }
}